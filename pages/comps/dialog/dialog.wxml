<view bindtap="onClose" class="dialog-wrap" hidden="{{!show}}">
    <view catchtap="onEmpty" class="dialog-content content-class">
        <slot></slot>
        <image bindtap="onClose" class="close-img" src="/assets/icon_close.png" wx:if="{{showClose}}"></image>
    </view>
</view>
